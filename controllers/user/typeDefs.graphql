type User {
  id: ID!
  username: String!
  email: String!
  firstName: String!
  lastName: String!
}

extend type Query {
  me: User
}

extend type Mutation {
  registerUser(
    username: String!
    password: String!
    email: String!
    firstName: String!
    lastName: String!
  ): User!
}
